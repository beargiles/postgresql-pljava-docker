# (borrowed from postgis/docker-postgis)
name: Docker PostgreSQL pl/java CI

on:
  push:
  pull_request:
  schedule:
    - cron: '15 5 * * 1'

defaults:
  run:
    shell: bash

jobs:

  make-docker-images:
    # strategy:
    #   matrix:
    #     postgres: [15]
    #     variant: [default]

    name: Build docker image for ${{ matrix.postgres }} variant ${{ matrix.variant }}
    runs-on: ubuntu-22.04
    # env:
    #   VERSION: ${{ matrix.postgres }}
    #   VARIANT: ${{ matrix.variant }}

    steps:
      name: Checkout source
      uses: actions/checkout@v3

    # name: Build docker image for ${{ env.VERSION }} ${{ env.VARIANT }}
    # run: make test

